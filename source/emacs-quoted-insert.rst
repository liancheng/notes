.. meta::
    :tags: emacs, quoted-insert

.. _emacs-quoted-insert:

###################
Emacs quoted-insert
###################

First press ``C-q``, then any key (combination) that you want to input.  For example: ``C-q TAB`` for a horizontal tab, and ``C-q RET`` for carriage return (represented as ``^M``).
